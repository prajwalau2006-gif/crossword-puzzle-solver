{
  "name": "crossword-solver-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": ".venv\\Scripts\\python -m uvicorn app.main:app --host 127.0.0.1 --port 5000 --reload",
    "test": ".venv\\Scripts\\python -m unittest app/tests.py",
    "build": "echo Backend build not needed"
  }
}
